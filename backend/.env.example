# Server port for Express (default 3000)
PORT=3000

# Toggle dev data mode (true uses generated data, false uses real sensors/DB)
DEV_MODE=false

# PostgreSQL connection settings
PG_HOST=
PG_PORT=
PG_USER=
PG_PASSWORD=
PG_DATABASE=
